# pip constraints: ultralytics 등 패키지가 onnxruntime (CPU 전용)을 의존성으로 끌어올 때
# GPU 버전과 충돌하는 것을 방지합니다.
# 사용법: uv pip install -r requirements.txt -c constraints.txt
#
# 방어 전략:
# 1. onnxruntime CPU 버전을 존재하지 않는 버전으로 고정 → 설치 차단
# 2. onnxruntime-gpu 버전을 고정
# 3. install.sh에서 설치 후 검증/복구

onnxruntime-gpu==1.24.4
# CPU 버전을 설치 불가능한 버전으로 고정하여 의존성 해결 시 차단
onnxruntime==99999.0.0
